import 'dart:ui';

import 'package:flutter/material.dart';

// Dark Theme

const darkPrimaryColor = Color(0xFF131C21); // Dark primary Color
const dMessageTextColor = Color(0xFFA9AAAC); //  Message Text Color in Chat List
const dChatBubbleColor = Color(0xFF056162); // Chat Bubble Color
const dSearchBarColor = Color(0xFF323739); // ̥Search Bar Color
const dUnreadMessageBadgeColor = Color(0xFFDCF8C6); // Unread Message Badge

// Light Theme

const lPrimaryColor = Color(0xFFFFFFFF); // Light Primary Color
const lMessageTextColor = Color(0xFFB2B2B2); // Message Text Color in Chat List
const lChatBubbleColor = Color(0xFFDCF8C6); // Chat Bubble Color
const lSearchBarColor = Color(0xFFFFFFFF); // Search Bar Color
const lUnreadMessageBadgeColor = Color(0xFFDCF8C6); // Unread Message Badge
